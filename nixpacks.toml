[phases.setup]
nixPkgs = ["python311", "glib", "pango", "cairo", "gdk-pixbuf", "libffi", "shared-mime-info", "fontconfig", "gobject-introspection"]

[phases.install]
cmds = ["python3 -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"]
